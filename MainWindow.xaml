<Window x:Class="Отдел_кадров.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Отдел_кадров"
        mc:Ignorable="d"
        Title="Контроль доступа" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Content="Логин:" Margin="10"/>
            <TextBox x:Name="LoginTextBox" Margin="10" Grid.Column="1"/>

            <Label Content="Пароль:" Margin="10" Grid.Row="1"/>
            <PasswordBox x:Name="PasswordPasswordBox" Margin="10" Grid.Row="1" Grid.Column="1"/>

        <Button x:Name="LoginButton" Content="Войти" Width="200" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" Click="LoginButton_Click"/>

    </Grid>
</Window>
